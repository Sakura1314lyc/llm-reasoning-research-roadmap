# 任务二：Agent 工具配置轻量检查表

这项任务的外部配置已经做得差不多，仓库里只留一份核对表，不自动安装或启动第三方 Agent：

- [x] 已理解 Agent Loop、Planning、Tool、Memory、Skill、MCP 与 Harness。
- [x] 已实现只读本地 Agent、MCP 示例和 pytest 测试。
- [ ] 在隔离环境分别运行 OpenClaw、Hermes、Codex、Claude Code。
- [ ] 记录每个工具的版本、模型 Provider、权限、Skill/MCP 配置和一次成功轨迹。
- [ ] 使用 GLM/DeepSeek 等 Provider 时，将 Token 放在本地环境变量，不提交仓库。

剩下的未勾选项在本地补记录即可。个人配置、密钥和机器路径不需要为了把清单勾满而上传。
